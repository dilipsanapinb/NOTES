

// const express = require('express');

// const app = express();

// const logError = (err,req, res, next) => {
//     if (req.url !== "/" || req.url !== "/users" || req.url !== "/users.userId") {
//         res.status(404).send("rout is not matching")
//     }
//     next()
// }
    
// app.use(logError);

// app.get('/', (req, res) => {
//     res.send("root path")
// });

// app.get('/users', () => {
//     res.send("users data")
// });

// app.get('/user/:userId', (req,res) => {
//     let id = req.params.userId;
// })

// app.listen(8000, () => {
//     console.log('Server is listening to port 8000');
// })




